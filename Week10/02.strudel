




$: stack(
  note("<e3 ~ g3 a3 b3>")
    .sound("gm_timpani")
    .gain(0.7).attack(0.01).release(0.9)
    .room(0.8).lpf(700).slow(2),
  note("<e4 g4 a4 b4>")
    .sound("supersaw")
    .cutoff(1200).gain(0.2)
    .attack(0.3).release(2)
    .room(0.9).delay(0.4).delaytime(0.5)
    .slow(2)
)
